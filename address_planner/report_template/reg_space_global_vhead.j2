`define {{ '%-60s' % ('ADDR_%s' % space.global_name.upper()) }} {{ ' ' * (60 - 40) }}{{space.hex_transform((space.global_start_address/8)|int)}}
{# Preserve the separator newline between recursive renders. #}
